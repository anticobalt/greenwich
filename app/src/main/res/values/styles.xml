<resources>

    <!-- Base application theme. -->
    <style name="DayTheme" parent="Theme.Cyanea.Light.DarkActionBar">
        <item name="defaultTextColor">@color/black</item>
        <item name="cardBgColor">@color/white</item>
    </style>
    <style name="NightTheme" parent="Theme.Cyanea.Light.DarkActionBar">
        <item name="defaultTextColor">@color/white</item>
        <item name="cardBgColor">@color/space</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
